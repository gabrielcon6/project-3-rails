import React from "react";
import Login from "../components/Login"

const landingPage = (props) => {

  // const user = props.user;

  return (
    <div>
        <h1>This is your landing (login) page!</h1>
        <Login />
    </div>
  );
  }


export default landingPage;