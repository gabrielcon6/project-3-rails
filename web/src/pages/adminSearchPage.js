import React from "react";

const landingPage = (props) => {

  // const user = props.user;

  return (
    <div>
        <h1>This is Lucy's page!</h1>
    </div>
  );
  }


export default landingPage;